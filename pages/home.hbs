<hr class="divider bg-white">

{{#each arr}}
            <div class="grid gap-3">
                <div class="card bg-dark text-white">
                    <img height="250px" src="{{url}}" class="card-img-top" alt="...">
                    <div class="card-body">
                        <h4>{{title}}</h4>
                        <p>{{content}}</p>
                           
                        <a href="" class="w-50 btn btn-primary">Read</a>
                    </div>
                </div>
                {{/each}}
     

           
            </div>